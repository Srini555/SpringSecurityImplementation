landon:
  guest:
    service.url: http://localhost:8100

spring:
  jpa:
    hibernate:
      ddl-auto: none
  ldap:
    embedded:
      ldif: classpath:landon.ldif
      base-dn: dc=frankmoley,dc=com
      port: 8389